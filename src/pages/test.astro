---
import Layout from "../layouts/Layout.astro";
import { getDict } from "../i18n/getDict";

const dict = await getDict("th");
---

<Layout t={dict} currentLang="th">
  <main class="min-h-screen bg-gray-50 pt-20">
    <div class="container mx-auto px-4 py-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-4">หน้าทดสอบปุ่ม Back</h1>
      <p class="text-gray-600 mb-4">หน้านี้ใช้ทดสอบปุ่ม back ใน navbar</p>
      <p class="text-gray-600 mb-4">ปุ่ม back ควรจะแสดงในมือถือเท่านั้น</p>
      
      <div class="bg-white p-6 rounded-lg shadow">
        <h2 class="text-xl font-semibold mb-2">วิธีทดสอบ:</h2>
        <ul class="list-disc list-inside space-y-1 text-gray-700">
          <li>ดูในมือถือ (หรือ responsive mode) จะเห็นปุ่ม back</li>
          <li>ดูใน desktop จะไม่เห็นปุ่ม back</li>
          <li>กดปุ่ม back จะกลับไปหน้าหลัก</li>
        </ul>
      </div>
    </div>
  </main>
</Layout>
